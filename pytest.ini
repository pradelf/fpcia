# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths =
    tests
pythonpath = .
log_cli = true
log_cli_level = INFO
addopts = -p no:warnings
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

